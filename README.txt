This is a quite flexible site scrapper implementation.
### Usage
By default it uses embedded Neo4j DB (application.properties regulates this behaviour)
/data URL is the main entry point for results retrieval
example: http://localhost:8080/data?url=https://www.pegaxis.com/services
more flexible data visualisation and testing are subject for the future